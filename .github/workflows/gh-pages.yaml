name: GitHub Pages

on:
    push:
        branches:
            - main

jobs:
    deploy:
        name: 🚀 Deploy on Github.io
        runs-on: ubuntu-latest
        concurrency:
            group: ${{ github.workflow }}-${{ github.ref }}
        steps:
            - name: ⬇️ Checkout repo
              uses: actions/checkout@v3

            - name: ⎔ Setup node
              uses: actions/setup-node@v3
              with:
                  node-version: 16

            # For this process we need the components to be in sync and reuse each other
            # so we need to link some of them together
            - name: 📥 Download deps
              run: |
                  rm package.json yarn.lock
                  for COMPONENT in `ls components`; do
                      if [ -d "components/${COMPONENT}" ]; then
                        echo ::group::..:: ${COMPONENT} ::..
                        cd components/${COMPONENT} && yarn install && yarn build && cd -;
                        echo ::endgroup::
                      fi  
                  done

            - name: 🥖 Build the React App
              run: |
                  cd docs/react-app && npm install && npm run build && cd -
                  cp docs/react-app/build/index.html docs/react-app/build/404.html

            - name: 🍿 Deploy
              uses: peaceiris/actions-gh-pages@v3
              if: ${{ github.ref == 'refs/heads/main' }}
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  publish_dir: ./docs/react-app/build
